name: "hello world demo"

on:
  push:
      branches: 
        -main

jobs:
   hello:
      runs-on: ubuntu-latest
      steps:
      - name: Checkout code
        uses: action/checkout@v3

      - name: Print Hello  
        run: echo "hello worlds,github action"

     
